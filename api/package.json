{
  "name": "bext-read-api",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only SELECT bridge so the cPanel-hosted dashboard can reach the VPS Postgres",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": { "pg": "^8.13.1" }
}
