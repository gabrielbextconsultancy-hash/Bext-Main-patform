.env
.env.local
*.local
node_modules/
.next/
out/
dist/
build/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
*.pem
*.key
!.env.example
infra/.env
scratch/
env.local.json
.env.backup-*

# Playwright MCP session artefacts — snapshots and console logs, not source
.playwright-mcp/

# Timestamped .env snapshots taken before automated rewrites — full secrets.
.env.bak.*

# FlowAgent runtime state — flow backup snapshots, not source.
.flowagent/

# Per-run pipeline output, not source: the extracted JSON and the built card.
graph/card.json
graph/extracted.json
