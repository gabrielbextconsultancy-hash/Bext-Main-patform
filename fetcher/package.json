{
  "name": "bext-fetcher",
  "version": "1.0.0",
  "private": true,
  "description": "Headless-browser fetch service for sources that refuse plain HTTP GETs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "playwright": "1.56.0"
  }
}
